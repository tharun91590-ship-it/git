 git config --global user.name 'tharun91590-ship-it'
git config --global user.email 'tharun91590@gmail.com'
git config user.name
git config user.email
git config --global init.defaultBranch main
git init
git status
git add readme.md
git commit -m 'commit_message'
git log
 git add .    //for all file in folder
git commit  -m "all"
 git checkout 7cefd9d96583c7581ec870f484889aec24e47d8e
git checkout main
git checkout -f main
git branch bug    // create a branch
git checkout bug
git checkout -b feature   //create a branch(2 step in one)
git push --set-upstream origin feature
git push -u origin main2.0   //short form of set upstream
git push
woq  // for  exit from git log
git pull    //pull from gtihub
git fetch   //pull branch from the remote repo in the GitHub
git merge main   //when conflict was created use this

